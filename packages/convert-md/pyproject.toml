[project]
name = "convert-md"
version = "0.1.0"
description = "Opinionated document→markdown conversion: multi-engine per-format fallback chains + a model-free fidelity grade."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
keywords = ["markdown", "conversion", "docling", "pandoc", "pdf", "docx"]
dependencies = [
    "docling>=2,<3",
    "pypandoc-binary>=1.17,<2",
    "markitdown>=0.0.2,<1",
    "python-pptx>=1.0.2,<2",
    "trafilatura>=1.9.0,<3",
    "html2text>=2025.4.15",   # date-versioned; allowlisted in tests/arch_allowlist.toml (no semver bound)
    "openpyxl>=3.1.5,<4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/convert_md"]
