seq = 29
date = "2026-07-08"
event = "verdict"
software = "settings-base"
release = "settings-base-v0.1.0"
actor = "a2web"
consumer = "a2web"
verdict = "adopted"
cost = "n/a"
note = "a2web's AppSettings composes settings-base (resolve_env_refs + resolve_config_path + SecretStrippingYamlSource), keeping its own secret-field set + path defaults; deleted the local machinery, kept tests green."
