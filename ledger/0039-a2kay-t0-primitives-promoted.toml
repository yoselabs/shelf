seq = 39
date = "2026-07-09"
event = "delivery"
software = "atomic-io, duckdb-sidecar, managed-region"
release = "atomic-io-v0.1.0, duckdb-sidecar-v0.1.0, managed-region-v0.1.0"
actor = "shelf"
consumer = "a2kay"
verdict = "pass"
cost = ""
note = "PROMOTE: closed docs/backlog.md's 'a2kay's T0 primitives' Promote item, the first package to walk the full PROMOTE cycle end-to-end (worktree, extract, tag, push, repoint, delete origin copy) rather than being bootstrapped. Extracted a2kay.core.{atomic_io,duckdb_sidecar,managed_region} verbatim (self-contained, zero a2kay imports, so no adaptation needed) into three T0 packages in a worktree (../shelf-a2kay, branch work/a2kay). Names kept as-is per resolution 0008 -- they already read as deliverables to a cold consumer. Two test-only fixes needed to clear the shelf's stricter make check: an EM101 (raise from a variable, not a literal) and three ty not-subscriptable errors (duckdb's fetchone() returns tuple | None; bound + asserted not-None before indexing) -- both pre-existing a2kay code passed a2kay's own weaker lint config uncaught. Repointed a2kay's 7 call sites at the git+tag sources, deleted the in-repo originals + their tests, added the three packages to a2kay's rego upper-bound allowlist (policies/data.json) alongside the existing 4. make check green on both sides: shelf 315 passed/88.77% cov, a2kay 866 passed/2 skipped. work/a2kay fast-forward-merged into shelf main and pushed; worktree + remote branch removed."
